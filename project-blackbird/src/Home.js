// import useFetch from "./useFetch";

const Home = () => {
  // const { error, isPending, data: blogs } = useFetch('http://localhost:8000/blogs')

  return (
    <div className="home">
    <h2>Homepage</h2>
    <p>This is the temporary homepage while Arijus works on the design.</p>
    </div>
  );
}
 
export default Home;